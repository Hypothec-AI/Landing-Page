import React from 'react';

// ValueProps component removed per request — keep a stub so imports don't break.
export function ValueProps({ isDark }: { isDark: boolean }) {
  // Section removed: "Why Mortgage Companies Choose Hypothec.AI" / Promise not rendered.
  return null;
}